node_modules
dist
.env
.env.local
.firebase
*.log
.DS_Store
tsconfig.tsbuildinfo

# Names WHICH project this deploys to — personal, not part of the code.
# Anyone who forks sets their own with `firebase use --add`.
.firebaserc
