rules_version = '2';

// Κάθε χρήστης διαβάζει και γράφει μόνο το δικό του προφίλ.
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
